# Minimal environment variables for local/dev

# Data source: sheet | gcs_json | bigquery
DATA_SOURCE=sheet

# Sheets (default when DATA_SOURCE=sheet)
# Required: spreadsheet ID that contains `Nodes` and `Edges` tabs
SHEET_ID_DEFAULT=1AbC…XYZ10y5y_3H-3qKzQmY8lx9wevIXXbkWfIU-Qx2uyaaw6Bg
SITE_ID_FILTER_DEFAULT=

# GCS JSON (optional; used when DATA_SOURCE=gcs_json or via query)
# Example: gs://my-bucket/path/graph.json or file:///ABS/PATH/graph.json
GCS_JSON_URI=

# BigQuery (optional; used when DATA_SOURCE=bigquery or via query)
BQ_PROJECT_ID=fr-tpd-sarpi-datagrs-dev
BQ_DATASET=testReseaux
BQ_NODES_TABLE=Nodes
BQ_EDGES_TABLE=Edges

# Embed configuration
EMBED_STATIC_KEY=dev-embed-key
# Space-separated hostnames allowed as Referer for embed endpoints
ALLOWED_REFERER_HOSTS="localhost 127.0.0.1"
# Space-separated origins allowed to frame the app
ALLOWED_FRAME_ANCESTORS="http://localhost:8000 http://127.0.0.1:8000"

# Dev toggles (only for local debugging)
DISABLE_EMBED_REFERER_CHECK=0
DISABLE_EMBED_KEY_CHECK=0

# Google Cloud / ADC (optional)
# Project and region (used by some deployments)
GCP_PROJECT_ID=fr-tpd-sarpi-datagrs-dev
GCP_REGION=europe-west1
# Impersonation:
# - If your local ADC are ALREADY impersonated to this SA (via
#   `gcloud auth application-default login --impersonate-service-account=...`),
#   leave this EMPTY.
# - Otherwise, set to the target SA email to let the app impersonate:
#   editeur-reseau-sa@${GCP_PROJECT_ID}.iam.gserviceaccount.com
IMPERSONATE_SERVICE_ACCOUNT=
